get:
  summary: Pour voir une collectivités grace à son ID
  tags:
    - Collectivites
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer
  responses:
    '200':
      description: Voici la collectivités
      content:
        application/json:
          schema:
            $ref: '../components/schemas/Collectivites.yaml'
    '404':
      description: Collectivite non trouvé
      content:
        application/json:
          schema:
            type: string
            example: "Collectivités non trouvé"
